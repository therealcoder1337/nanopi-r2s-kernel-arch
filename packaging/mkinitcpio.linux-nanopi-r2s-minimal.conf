# Headless mkinitcpio for NanoPi R2S — pacman package linux-nanopi-r2s-minimal.
# Uses classic udev initramfs discovery; no systemd, keyboard, or KMS hooks.

MODULES=()
BINARIES=()
FILES=()

HOOKS=(base udev autodetect modconf block filesystems fsck)

COMPRESSION="zstd"
